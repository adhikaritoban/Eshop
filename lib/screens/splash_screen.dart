import 'package:flutter/material.dart';

class SplashScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    print('splash auto login');
    return Scaffold(
      body: Center(
        child: Text('Loading...'),
      ),
    );
  }
}